services:
  app:
    build: .
    container_name: ai20k-php
    environment:
      DB_DRIVER: sqlite
      SQLITE_PATH: /var/www/html/data/quiz.db
    ports:
      - "80:80"
      - "8082:80"
    volumes:
      - quiz_data:/var/www/html/data
    restart: unless-stopped

volumes:
  quiz_data:
